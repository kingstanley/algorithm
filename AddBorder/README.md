Given a rectangular matrix of characters, add a border of asterix(*) to it

Example:
for
picture = [
    "abc",
    "ded"
]
the output should be
addBorderPicture = ["*********",
"*abc*",
"*ded*",
"**********"
]

Hint: 
concat
unshift
push